# friends-image-recogonition
An AI Face Recognition System built with the face_recognition library, OpenCV, and NumPy. Trained on a custom dataset, it extracts 128-dimensional facial embeddings for classification. Uses an interactive ipywidgets interface for user uploads, drawing dynamic bounding boxes with predicted names, and saves data into a production-ready .pkl file.
